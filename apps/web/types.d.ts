import '@foundation-trpc/network/next-auth' // even thow we are use at network folder (we added another value to our third party lib "next-auth")
// we had to import it. because when we trying to commit. there mansioning this mismatch issue (ts issue).
